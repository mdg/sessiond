<erl>
out(A) ->
	{ok, SessionID} = yaws_api:getvar(A, sessionid),
	{Success, Live} = sessiond:serve(live, SessionID),
	{html, "{\""++ erlang:atom_to_list(Success) ++"\":"++ erlang:atom_to_list(Live) ++"}"}.
</erl>

